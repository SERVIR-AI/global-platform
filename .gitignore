# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv
# (uv.lock is committed on purpose; keep it tracked)

# Env / secrets
.env
*.env
!*.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# local data cache (OSM + downloaded/clipped tifs) — fetched at runtime
cache/

e2e_*.json
Copy of Data List_ASEAN.xlsx

# local-only working tracker (kept out of the repo on purpose)
BUILD_PLAN.md

cache/
CLAUDE.md

# local-only food-security planning, reference docs, sample configs, and issue tracker
# (built out later; progress tracked in docs/food-security/ISSUE-TRACKER.md)
docs/food-security/

# local-only internal playbooks (repo standards, etc.) — never pushed
docs/internal/

# reference PDFs (not part of the build)
SERVIR Global Platform PoC.pdf
screencapture-localhost-5173-2026-06-29-19_41_17.pdf