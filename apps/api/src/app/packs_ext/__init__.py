"""Contributed domain packs — droppable modules, each exporting a SPEC dict.

Scaffold one with:  uv run python -m app.contrib.cli new-pack <id>
Validate it with:   uv run python -m app.contrib.cli doctor <id>
"""
